<UserControl x:Class="LaunchPad.Icon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LaunchPad"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="80">
    <StackPanel MaxWidth="90">
        <Border CornerRadius="18" Width="80" Height="80" Name="appIcon" Background="{StaticResource LaunchPadItemBackground}" Padding="3">
            <Border.RenderTransform>
                <ScaleTransform />
            </Border.RenderTransform>
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
                </Style>
            </Border.Style>
            <Image Name="iconBitmap" Width="auto" Height="auto" Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality"/>
        </Border>
        <TextBlock Foreground="{StaticResource LaunchPadTextColor}" Visibility="Collapsed" Margin="0,5,0,0" HorizontalAlignment="Center" FontSize="13" x:Name="appName">App</TextBlock>
    </StackPanel>


</UserControl>
