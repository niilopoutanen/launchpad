<UserControl x:Class="LaunchPad.Apps.Suggestion"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LaunchPad.Apps"

             mc:Ignorable="d">
    <Border Width="150" Height="80" Name="Container" Padding="8"
                CornerRadius="18" BorderThickness="2" Margin="5">
        <Border.RenderTransform>
            <ScaleTransform />
        </Border.RenderTransform>
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
            </Style>
        </Border.Style>
        <Border.Background>
            <ImageBrush ImageSource="/Resources/Assets/dashed_border.png"/>
        </Border.Background>
        <TextBlock Name="SuggestionText" TextWrapping="Wrap"
                   TextAlignment="Center" VerticalAlignment="Center"
                   FontSize="14" Foreground="{StaticResource LaunchPadTextColor}"
                   FontFamily="{StaticResource Inter}"/>

    </Border>
</UserControl>
